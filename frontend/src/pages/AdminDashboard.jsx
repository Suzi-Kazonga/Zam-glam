import { useAuth } from '../context/AuthContext';

export default function AdminDashboard() {
  const { user } = useAuth();
  return <section className="mx-auto max-w-7xl px-6 py-12"><p className="text-sm font-semibold uppercase tracking-widest text-indigo-600">Operations</p><h1 className="mt-2 text-3xl font-bold text-slate-900">Admin dashboard</h1><p className="mt-2 text-slate-500">Manage the Zamglam marketplace from one place.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{[['Users','1,248','Registered accounts'],['Stores','86','Active sellers'],['Products','4,392','Live listings']].map(([label,value,detail]) => <div key={label} className="rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold text-slate-900">{value}</p><p className="mt-1 text-sm text-slate-400">{detail}</p></div>)}</div><div className="mt-8 rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-xl font-bold text-slate-900">Welcome, {user?.name}</h2><div className="mt-5 grid gap-3 sm:grid-cols-3"><button className="rounded-lg bg-indigo-600 px-4 py-3 font-semibold text-white hover:bg-indigo-700">Manage users</button><button className="rounded-lg border border-slate-300 px-4 py-3 font-semibold text-slate-700">Review stores</button><button className="rounded-lg border border-slate-300 px-4 py-3 font-semibold text-slate-700">Audit products</button></div></div></section>;
}
