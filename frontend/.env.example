# Relative on purpose: requests go to whatever host served the page, then through
# the Vite dev proxy to the backend. An absolute http://localhost:5000 URL breaks the
# site on a phone, where "localhost" is the phone itself.
VITE_API_URL=/api
# VITE_COURIER_API_URL is unused by the app; the backend talks to the courier service.
